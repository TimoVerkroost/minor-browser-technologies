(function () {
    const supportChecker = document.getElementById("support");
    console.log(supportChecker);
    //supportChecker.innerHTML = "This browser does support ES6 so the const deceleration works here.";
}());